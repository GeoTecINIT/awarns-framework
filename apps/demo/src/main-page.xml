<Page xmlns="http://schemas.nativescript.org/tns.xsd" navigatingTo="navigatingTo" class="page">
  <Page.actionBar>
    <ActionBar title="Plugin Demos" icon="" class="action-bar"/>
  </Page.actionBar>
  <StackLayout class="p-20">
    <ScrollView class="h-full">
      <StackLayout>
        <Button text="core" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
        <Button text="persistence" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
        <Button text="tracing" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
        <Button text="battery" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
        <Button text="geolocation" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
        <Button text="wifi" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
        <Button text="geofencing" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
        <Button text="human-activity" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
        <Button text="notifications" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
        <Button text="ble" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
        <Button text="phone-sensors" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
      </StackLayout>
    </ScrollView>
  </StackLayout>
</Page>
